#!/usr/bin/env bash
# this script configures a new server
sudo apt-get -y update
sudo apt-get -y install nginx
sudo sed -i "36 i\ \tadd_header X-Server-By \$hostname;" /etc/nginx/sites-enabled/default
sudo service nginx restart
